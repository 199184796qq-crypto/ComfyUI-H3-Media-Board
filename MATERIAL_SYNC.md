# H3 素材同步桥

重启 ComfyUI 并刷新浏览器，搜索「H3 素材同步桥」。

1. 将 H3-Media-Board 的 `media_board` 输出连接到同步桥输入。
2. 在同步桥的「目标时间线」中，按节点编号选择 Timeline Director / Material Planner。
3. 点击「同步素材」，等待状态显示完成，再运行生成。也可开启「自动同步」，素材清单变化后约 1.5 秒开始检查。

目标通过下拉框指定，不需要修改或给第三方节点新增输入口。同步桥没有输出口；目标的 `timeline_data` 不应接线。旧版本「导入素材清单」输出已移除，加载旧工作流时会清除该输出及其连线；请重新点击同步后再生成。

仅同步图片、视频、音频和生成视频的宽、高、时长。宽高复用素材板的原始分辨率计算（不是二次放大尺寸），时长使用素材板的「时长」值（不是手动帧数换算值）。图片、音频加入素材区；新增视频依次追加到视频轨末尾。目标生成范围保留起点并更新时长。提示词、采样设置及其它参数不传递。目标宽、高或时长由连线控制时会提示断开后重试。

素材按来源节点和槽位对应，替换相同槽位会更新素材并保留引用；已有视频保留位置、裁剪和参考模式。不要通过重新排列来源槽位来表示同一素材。

来源删除素材时目标保留原素材。目标手动删除同步素材后，可点击同步重新导入。同步后超过目标 9 图 / 3 音频容量、替换视频短于已有剪辑范围或接口不兼容时，整批不写入目标，自动同步暂停。修正后手动重试或重新开启自动同步。

文件复制到 `input/minimax_h3_timeline_director`，同一文件版本复用副本；更新产生新副本，旧副本不会自动清理。仅在浏览器工作流打开时自动同步；直接 API 提交工作流不会更新编辑器。请在点击生成前等同步完成。自动检测素材清单变化，原路径文件内容被外部覆盖时请手动同步。

## 第三方升级与维护

功能随本仓库更新，不修改第三方源码。第三方相关假设集中在 `web/timeline_sync_adapter.js`：节点类型、v4 编辑状态、`__m3td` 的 `sync/render` 方法及 `/minimax_h3_timeline/media_info` 接口。文件导入目录在 `material_sync.py`。

当前依据本机 Timeline Director 源码适配。它尚无正式的外部编辑器导入接口，因此仍依赖内部接口；结构检查不能保证未来所有版本兼容。升级第三方后先在工作流副本中手动同步少量图片、音频、视频，检查预览、裁剪和分段引用。若失败，保留现有工作流，更新本仓库适配器或恢复此前第三方版本。

开发验证：`python tests/test_material_sync.py`、`node tests/test_timeline_sync.mjs`。后续建议向第三方提交稳定的素材导入接口，以减少内部接口依赖。
