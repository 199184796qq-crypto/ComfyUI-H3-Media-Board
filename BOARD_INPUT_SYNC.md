# H3 图片音频无线同步桥

已安装到本机 `E:\AI\COMFYUIdapaoVIP\comfyui313\custom_nodes\ComfyUI-H3-Media-Board`。

## 使用
1. 重启 ComfyUI，然后刷新网页。搜索 **H3 图片音频无线同步桥**（H3-Media-Board 分类）。
2. 把图片处理节点的 IMAGE 输出接到桥的 image；音频处理节点的 AUDIO 输出接到 audio。两种可同时接，也可只接一种。若保存音频节点没有输出口，从它前面的音频处理节点分出一条线。
3. 在“目标素材板 ID”下拉框选择目标 H3 Media Board。可点击“定位目标素材板”核对。
4. 设置图片序号 1–9、音频序号 1–3。序号相互独立；批次索引默认 0，即使用批次中的第一项。
5. 点击 ComfyUI 的运行按钮。桥与目标素材板之间不需要连线；同步完成后对应卡片显示新素材，随后保存工作流。

排队时前端会自动建立内部执行依赖，使目标素材板及其下游在本次运行就能使用新素材。界面没有可见连线。运行整个工作流也会执行已启用的下游生成节点。

桥上的“同步清单”输出和内部 base_manifest 参数由程序处理，无需手工连接。不是接上线就立即传送：图片/声音须经过一次运行才能得到数据。支持多个桥同步同一素材板的不同槽位；同一槽位发生冲突时会阻止排队。目标素材板的输出或下游不能反过来作为自己的同步来源。

只覆盖指定槽位，不清空其他图片、音频、视频和提示词。图片保存为 PNG，音频保存为 16 位 PCM WAV，保留采样率和声道。文件位于 input/h3_media_board，按内容去重；保存工作流不会把媒体文件嵌入 JSON，迁移时须一并复制媒体目录。

## 验收步骤
1. 新建一个测试工作流，添加 H3 Media Board、加载图片、加载音频、无线同步桥；无需接视频生成节点。
2. 先手工给素材板第 1 张图放一张旧图。
3. 桥接入另一张图片和一段音频，目标选该素材板，图片序号设 9、音频序号设 3，运行。
4. 检查图 9 更新、音频 3 可播放、图 1 保留；中间空槽位不得被挤到前面。
5. 保存工作流并重新打开，检查槽位、目标选择与媒体仍然存在。
6. 增加第二个桥写入图 2，运行后检查两个桥的素材均存在；两个桥都写图 9 时应提示冲突。

## 已完成验证与限制
已通过本机 Python 实际 PNG/WAV 保存读取、空槽补齐、旧槽保留、内容去重、批次越界检查；已通过 JavaScript 语法及无线依赖、多桥串联、重复槽位、循环和目标缺失检查。
当前运行中的 ComfyUI 未重启，因此尚未验证重启后的真实界面展示及完整排队执行。网页保持打开时才会更新可见卡片；纯 API 调用必须提供 base_manifest 并自行建立依赖，不能只传目标 ID。目标选择目前针对当前主画布上的素材板。

## 改动
新增 board_input_sync.py、web/board_input_sync.js，并在 __init__.py 注册节点。原有“素材板→时间线”同步桥保留。
